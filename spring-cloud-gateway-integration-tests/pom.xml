<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xmlns="http://maven.apache.org/POM/4.0.0"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<artifactId>spring-cloud-gateway-integration-tests</artifactId>
	<packaging>pom</packaging>

	<name>Spring Cloud Gateway Integration Tests</name>
	<description>Spring Cloud Gateway Integration Tests</description>

	<properties>
	</properties>

	<!--
	定位：集成测试模块
	作用：包含网关的端到端测试用例，验证网关与服务发现、负载均衡、安全组件等的集成功能。
	学习价值：参考测试用例的写法，学习如何用 WebTestClient 测试网关路由。
	-->

	<parent>
		<groupId>org.springframework.cloud</groupId>
		<artifactId>spring-cloud-gateway</artifactId>
		<version>3.1.11-SNAPSHOT</version>
		<relativePath>..</relativePath> <!-- lookup parent from repository -->
	</parent>
	
	<modules>
		<module>grpc</module>
		<module>http2</module>
		<module>mvc-failure-analyzer</module>
	</modules>

	<build>
		<plugins>
			<plugin>
				<artifactId>maven-deploy-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>

